/**
 * Post-processing hologram effects (bloom, scanline, noise)
 * have been removed so the mannequin reads as a clean studio
 * render. The component stays mounted to preserve the scene
 * graph structure in CanvasScene.
 */
export default function Effects() {
    return null;
}
